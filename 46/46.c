#include <stdio.h>
#include <math.h>

int main() {
    char note = 'z';
    int Average, A, B, C, D;
    for (; note == 'h'|| not == 'H';)
    {
        printf("Harf notunu giriniz (Cikis icin H ya da h giriniz) : ");
        scanf("%c",note);
        switch (note)
        {
        case 'a' || 'A':
            
            break;
        
        default:
            break;
        }
        
    }
    
    return 0;
}
